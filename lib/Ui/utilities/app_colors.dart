import 'package:flutter/material.dart';

const Color backgroundColor = Color(0xFFFFBD29);
const Color formFieldFillColor = Color(0xFFFFE5AB);

const Color adminBackgroundColor = Color(0xFF222725);

// gradients

const LinearGradient pinkRed = LinearGradient(
    begin: Alignment.centerLeft,
    end: Alignment.centerRight,
    colors: [Colors.pink, Colors.red]);

const LinearGradient pinkOrange = LinearGradient(
    begin: Alignment.centerLeft,
    end: Alignment.centerRight,
    colors: [Colors.pink, Colors.orange]);

const LinearGradient purpleBlue = LinearGradient(
    begin: Alignment.centerLeft,
    end: Alignment.centerRight,
    colors: [Colors.purple, Colors.blue]);

const LinearGradient purpleLime = LinearGradient(
    begin: Alignment.centerLeft,
    end: Alignment.centerRight,
    colors: [Colors.deepPurpleAccent, Colors.lime]);
